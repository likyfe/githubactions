name: helloworld
on:
  push:
    branches: [ main ]

jobs:
    hello-job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Hola Mundo
              run: echo "Hola desde helloworld.yml - GitHub Actions funcionando"
